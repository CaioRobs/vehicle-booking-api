version: "3.9"
services:  
  app:
    build: .
    env_file:
      - .env
    restart: always
    depends_on:
      - database
    ports:
      - "3000:3000"
  
  database:
    image: mongo:latest
    ports:
      - '27017:27017'
  
